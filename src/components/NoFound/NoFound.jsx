import React from 'react'
import styles from './NoFound.module.scss'

const NoFound = () => (
  <div className={ styles.NoFound }>
    <div className={ styles.NoFoundImage }></div>
  </div>
)

export default NoFound
